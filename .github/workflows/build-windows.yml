name: Build Fingerprint Chromium Linux

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 480

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          pip3 install httplib2 PySocks six colorama

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git curl lsb-release \
            pkg-config build-essential \
            libglib2.0-dev \
            python3-pip \
            python3-setuptools \
            xz-utils \
            patch \
            ninja-build

      - name: Install depot_tools
        run: |
          git clone https://chromium.googlesource.com/chromium/tools/depot_tools.git $HOME/depot_tools
          echo "$HOME/depot_tools" >> $GITHUB_PATH

      - name: Set environment
        run: |
          echo "DEPOT_TOOLS_UPDATE=0" >> $GITHUB_ENV
          echo "PYTHONDONTWRITEBYTECODE=1" >> $GITHUB_ENV

      - name: Download Chromium source
        run: |
          mkdir -p build/download_cache
          python3 utils/downloads.py retrieve \
            -i downloads.ini \
            -c build/download_cache

      - name: Unpack Chromium source
        run: |
          python3 utils/downloads.py unpack \
            -i downloads.ini \
            -c build/download_cache \
            -- build/src

      - name: Prune binaries
        run: |
          python3 utils/prune_binaries.py build/src pruning.list

      - name: Apply patches
        run: |
          python3 utils/patches.py apply build/src patches

      - name: Substitute domains
        run: |
          python3 utils/domain_substitution.py apply \
            -r domain_regex.list \
            -f domain_substitution.list \
            -c build/domsubcache.tar.gz \
            build/src

      - name: Install Chromium build dependencies
        run: |
          sudo build/src/build/install-build-deps.sh \
            --no-syms --no-arm --no-chromeos-fonts --no-nacl

      - name: Setup build configuration
        run: |
          mkdir -p build/src/out/Default
          cp flags.gn build/src/out/Default/args.gn
          cd build/src
          out/Default/gn gen out/Default --fail-on-unused-args

      - name: Build Chromium
        run: |
          cd build/src
          ninja -C out/Default chrome chromedriver chrome_sandbox

      - name: Package output
        run: |
          cd build/src
          zip -r ../../fingerprint-chromium-linux-x64.zip \
            out/Default/chrome \
            out/Default/chrome_sandbox \
            out/Default/chromedriver \
            out/Default/chrome_crashpad_handler \
            out/Default/*.so \
            out/Default/*.pak \
            out/Default/icudtl.dat \
            out/Default/v8_context_snapshot.bin \
            out/Default/locales \
            out/Default/resources

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: fingerprint-chromium-linux-x64
          path: fingerprint-chromium-linux-x64.zip
          retention-days: 7
